name: my-action
on: push
jobs: 
  run-aplication:
   runs-on: ubuntu-latest
   steps:
      - name: access git
        uses: actions/checkout@v3
      - name: install dependencies
        run: npm ci
      - name: run init
        run: npm run init
      - name: run lint
        run: npm run lint
      - name: run test 
        run: npm run test
      - name: Run build
        run: npm run build
      
